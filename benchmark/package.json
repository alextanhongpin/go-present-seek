{
  "name": "benchmark",
  "version": "1.0.0",
  "description": "This runs a benchmark for 10 seconds, using 10 threads, and keeping 100 HTTP connections open.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3"
  }
}
